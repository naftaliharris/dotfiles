#!/usr/bin/env python

import sys
from math import sqrt
import matplotlib.pyplot as plt

xs = []
ys = []
for line in sys.stdin.readlines():
    x, y = line.split()
    xs.append(float(x))
    ys.append(float(y))

plt.plot(xs, ys)
plt.show()
